import { Base } from "../elements/base.element";
export declare class MouseService {
    private _selected;
    private container;
    tool: string;
    constructor();
    set selected(selected: Base);
}
